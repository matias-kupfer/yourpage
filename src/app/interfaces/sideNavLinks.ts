export interface SideNavLinks {
  matIcon: string;
  iconColor: string;
  text: string;
  componentName: string;
}

export interface SideNavActions {
  matIcon: string;
  iconColor: string;
  text: string;
  action: string;
}
